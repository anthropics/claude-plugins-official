---
description: "List beads issues"
argument-hint: "[-s STATUS] [-t TYPE] [-a ASSIGNEE] [-l LABELS] [--all]"
allowed-tools: ["Bash(bd list:*)"]
---

Run `bd list` with the provided arguments:

```!
bd list $ARGUMENTS
```
