---
description: "Show beads issue details"
argument-hint: "ID [--json] [--children] [--refs]"
allowed-tools: ["Bash(bd show:*)"]
---

Run `bd show` with the provided arguments:

```!
bd show $ARGUMENTS
```
