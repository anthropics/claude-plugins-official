---
description: "Show ready beads issues (no blockers)"
argument-hint: "[-a ASSIGNEE] [-t TYPE] [-p PRIORITY] [--pretty]"
allowed-tools: ["Bash(bd ready:*)"]
---

Run `bd ready` with the provided arguments:

```!
bd ready $ARGUMENTS
```
