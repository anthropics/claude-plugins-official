---
description: "Show beads dependency graph"
argument-hint: "[ID] [--all] [--compact]"
allowed-tools: ["Bash(bd graph:*)"]
---

Run `bd graph` with the provided arguments:

```!
bd graph $ARGUMENTS
```
