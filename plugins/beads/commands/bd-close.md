---
description: "Close a beads issue"
argument-hint: "ID [-r REASON] [--suggest-next]"
allowed-tools: ["Bash(bd close:*)"]
---

Run `bd close` with the provided arguments:

```!
bd close $ARGUMENTS
```
