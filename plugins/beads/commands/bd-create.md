---
description: "Create a new beads issue"
argument-hint: "TITLE [-d DESCRIPTION] [-t TYPE] [-p PRIORITY] [-l LABELS] [--deps DEPS]"
allowed-tools: ["Bash(bd create:*)"]
---

Run `bd create` with the provided arguments:

```!
bd create $ARGUMENTS
```
