"""plugins/ — executable, code-level country plugin implementations.

Distinct from `countries/<code>/`, which holds each country's markdown/YAML
*specification* (config, legal sources, MCP bindings, knowledge). A
directory under `plugins/` reads that data and implements real behavior
against the contracts in `core.engine.plugin_engine.contracts`.
"""
