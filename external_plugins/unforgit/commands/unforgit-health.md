# Unforgit Health

Inspect Unforgit memory health.

Run `unforgit_health` and `unforgit_sync_status`. Report actionable problems only: missing embeddings, duplicate consolidation candidates, sync failures, invalid config, or repository initialization issues.
