# Unforgit

Show Unforgit repository-memory status and suggest the next useful action.

1. Use `unforgit_health` to inspect local memory quality.
2. Use `unforgit_sync_status` to inspect sync/embedding status.
3. Summarize issues, warnings, and useful commands briefly.
4. If the repository is not initialized, tell the user to run `unforgit init`.
